import { createApp } from 'vue' // Importing createApp function
import App from './App.vue' // Importing App component

createApp(App).mount('#app')     // Mounting App component to the div with id app
